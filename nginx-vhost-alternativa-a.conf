# =============================================================================
# landing-au — Virtual Host Nginx (Alternativa A: Puerto Localhost)
# =============================================================================
# Ubicación: /etc/nginx/sites-available/landing.conf
# Enlace: sudo ln -s /etc/nginx/sites-available/landing.conf /etc/nginx/sites-enabled/
# =============================================================================
# Asume: Nginx global preinstalado en /etc/nginx/nginx.conf
# Asume: Docker expone 127.0.0.1:3000 (solo localhost)
# =============================================================================

upstream landing_backend {
    server 127.0.0.1:3000;
    keepalive 32;
}

server {
    listen 80;
    listen [::]:80;
    server_name landing.midominio.com;

    # Logs específicos para este virtual host
    access_log /var/log/nginx/landing-access.log;
    error_log /var/log/nginx/landing-error.log;

    # Gzip para mejor performance
    gzip on;
    gzip_vary on;
    gzip_proxied any;
    gzip_comp_level 6;
    gzip_types
        text/plain
        text/css
        text/xml
        text/javascript
        application/json
        application/javascript
        application/xml+rss
        application/atom+xml
        image/svg+xml;

    # Client max body size para formularios futuros
    client_max_body_size 16M;

    # Proxy a Next.js en localhost:3000
    location / {
        proxy_pass http://landing_backend;
        proxy_http_version 1.1;

        # WebSocket support (necesario para Server Actions de Next.js)
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";

        # Headers estándar de proxy
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-Host $host;
        proxy_set_header X-Forwarded-Port $server_port;

        # Timeouts
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;

        # Buffering
        proxy_buffering on;
        proxy_buffer_size 4k;
        proxy_buffers 8 4k;
        proxy_busy_buffers_size 8k;
    }

    # Let's Encrypt — descomentar cuando se configure certbot
    # location /.well-known/acme-challenge/ {
    #     root /var/www/certbot;
    # }
}

# =============================================================================
# HTTPS — Descomentar después de configurar Let's Encrypt
# =============================================================================
# server {
#     listen 443 ssl http2;
#     listen [::]:443 ssl http2;
#     server_name landing.midominio.com;
#
#     access_log /var/log/nginx/landing-access.log;
#     error_log /var/log/nginx/landing-error.log;
#
#     # SSL Certificates
#     ssl_certificate /etc/letsencrypt/live/landing.midominio.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/landing.midominio.com/privkey.pem;
#
#     # SSL Security
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384;
#     ssl_prefer_server_ciphers off;
#     ssl_session_cache shared:SSL:10m;
#     ssl_session_timeout 1d;
#     ssl_session_tickets off;
#
#     # HSTS — activar SOLO después de verificar HTTPS funciona correctamente
#     # add_header Strict-Transport-Security "max-age=63072000; includeSubDomains; preload" always;
#
#     location / {
#         proxy_pass http://landing_backend;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection "upgrade";
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_set_header X-Forwarded-Host $host;
#         proxy_set_header X-Forwarded-Port $server_port;
#         proxy_connect_timeout 60s;
#         proxy_send_timeout 60s;
#         proxy_read_timeout 60s;
#     }
# }
